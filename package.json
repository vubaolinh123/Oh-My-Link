{
  "name": "oh-my-link",
  "version": "0.10.5",
  "description": "Multi-agent orchestration for Claude Code — built from Claude Code source code leak",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "node test/run-tests.mjs",
    "test:all": "node test/run-all.mjs",
    "prebuild": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.5.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0"
  }
}
